Notas:

La configuración de superset esta en el import en superset conf.

para la instalación mirar el archivo que esta en docs instalations.rst
para hacer cambios mirar el archivo CONTRIBUTING.md

la base de datos queda guardada en DATA_DIR = os.path.join(os.path.expanduser('~'), '.superset').

de la linea 83 del archivo bootswatch.less quite una coma. antes del parentesis.

El logo se cambia en el archivo de configuración que está en los imports.

La modificación para que reciba porcentaje se hizo en viz.py en la linea 257.

Para modificar el color esta en Superset/superset/static/assets/javascript/modules/colors.js

Para hacer prints en js:
(function(){console.log(payload.form_data.bar_stacked); return 1})()

Para rotar la grafica .attr("transform","rotate(45)") nvd3_vis.js en la linea 123
Para rotar los labels es en la linea 123, 204, 315 y 458 en el render

db:
la base de datos está en la carpeta de database_dashboards es de sqlite3 y se pone en una carpeta oculta que se llama
.superset
